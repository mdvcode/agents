# <Project Name>

Private agent memory for this project.

## Target Repository
- Local path:
- GitHub URL:
- Default branch:

## Issue Workflow
1. User gives project and issue number.
2. Agent reads `privacy.md`.
3. Agent creates or switches to `codex/issue-<number>-<short-name>`.
4. Agent creates `issues/issue-<number>.md` from `docs/issues/_template.md`.
5. Agent works through artifacts, checks, review, and verdict.
6. Agent copies final summary into the issue journal.
7. Agent promotes durable knowledge into project memory/wiki.

## Project Memory
- `issues/`: private issue execution history.
- `memory/`: cross-issue memory.
- `wiki/`: curated project knowledge.
- `graph/`: project maps.
