# Scratchpad

Short-lived reminders for future agent-system improvements.

## Open
- Add a command that creates branch, issue journal, and kanban card from a GitHub issue number.
- Consider optional semantic search over `docs/` if exact `rg` search becomes insufficient.
- Add examples of completed issue journals after the first real GitHub issue run.

## Done
- Added wiki, memory, graph, goal template, schemas, Makefile, and new skills.
