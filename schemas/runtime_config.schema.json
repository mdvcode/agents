{
  "required": ["provider", "executor_command", "transport", "api_required", "model_router", "default_execution_profile", "execution_profiles"],
  "enums": {
    "provider": ["codex-sdk", "codex-cli"],
    "transport": ["local_subscription"]
  },
  "types": {
    "provider": "str",
    "executor_command": "str",
    "transport": "str",
    "api_required": "bool",
    "model_router": "bool",
    "default_execution_profile": "str",
    "execution_profiles": "dict"
  }
}
